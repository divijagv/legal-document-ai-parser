[project]
name = "legal-document-ai-parser"
version = "0.1.0"
description = "AI-powered extraction of structured data from legal subpoenas, summons, and court orders."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-adk>=1.22.0",
    "google-genai>=1.57.0",
    "python-dotenv>=1.0.0",
]
